// condicional if ==> Si...
let edad = 18;

// sintaxis del if
// palabra reservada if + condición entre paréntesis + bloque de código entre llaves
/* if (condicion) {
  // bloque de código a ejecutar si la condición es verdadera
} */

if (edad ==18) {  // operador de comparación ==

  console.log("Puedes comprar bebidas == 18" + edad);  
 }

 if (edad >18) {  // operador de comparación ==

  console.log("Puedes comprar bebidas > 18" + edad);  
 }

 if (edad >= 18 ) {  // operador de orden mayor o igual que

  console.log("Puedes comprar bebidas ≥ 18" + edad);  
 }
 if (edad < 0) {  // operador de orden menor que

  console.log("Corrije el campo edad < 0" + edad);  
 }

let password = "admi235"; // string: cadena de texto
console.log("El password tiene " + password.length + " caracteres");
if (password.length < 6 && password.length == 15) {  // operador de orden mayor o igual que

  console.log("Bienvenido, tu password es seguro" );  
 } else {

  console.log("El pasword debe tener entre 6 y 15 caracteres" );  
 }
     // operador de orden mayor que



 // Ejemplos de viajes
 let pasaporteVigente = true;
 let pasajeComprado = true;